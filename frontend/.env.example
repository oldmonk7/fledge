# Backend API URL. Leave empty to use Next.js rewrites (local dev).
# Production: use full URL (https://your-backend.up.railway.app) or hostname only (your-backend.up.railway.app).
NEXT_PUBLIC_API_URL=
